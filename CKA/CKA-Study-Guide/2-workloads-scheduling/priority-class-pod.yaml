apiVersion: v1
kind: Pod
metadata:
  name: critical-agent
spec:
  priorityClassName: high-priority
  containers:
  - name: agent
    image: busybox
    command: ["sh", "-c", "while true; do echo running; sleep 5; done"]

